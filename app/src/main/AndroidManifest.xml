<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jhordyabonia.ag"
    android:versionCode="002"
    android:versionName="Genesis">

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="chat.ListChatActivity"
            android:icon="@drawable/ic_chat"
            android:label="@string/title_activity_list_chat_beta">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="chat.ChatActivity"
            android:icon="@drawable/ic_chat_menu" />
        <activity android:name="crud.AlertaActivity" />
        <activity android:name="crud.AsignaturaActivity" />
        <activity android:name="crud.CalificableActivity" />
        <activity android:name="crud.LecturaActivity" />
        <activity android:name="crud.HorarioActivity" />
        <activity android:name="crud.ApunteActivity" />
        <activity android:name=".InformacionActivity" />
        <activity android:name="chat.ProfileActivity" />
        <activity android:name=".RecuperarCuentaActivity" />

        <service
            android:name="util.UploadService"
            android:enabled="true" />
        <service
            android:name="chat.ChatService"
            android:enabled="true" />

        <receiver android:name="util.Alarma" />

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>